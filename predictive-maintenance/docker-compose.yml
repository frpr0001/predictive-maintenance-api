version: "3.9"

services:
  api:
    build: .
    container_name: predictive-maintenance-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data   # Persistér SQLite database
    restart: unless-stopped
